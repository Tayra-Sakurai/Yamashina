<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Yamashina.Views.PaymentDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Yamashina.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
          Padding="12,16"
          RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Uid="DetT" Style="{StaticResource TitleTextBlockStyle}"/>
        <TextBlock x:Uid="Desc" Style="{StaticResource BodyTextBlockStyle}"
                   Grid.Row="1"/>
        <TextBox x:Uid="Method" Text="{x:Bind viewModel.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="2"/>
        <CommandBar Grid.Row="3">
            <AppBarButton Icon="Save"
                          x:Uid="Sv"
                          Command="{x:Bind viewModel.SaveCommand}"/>
        </CommandBar>
    </Grid>
</Page>
